import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  userPhoto: {
    width: 40,
    height: 40,
    resizeMode: 'cover',
  },
  chatBalloon: {
    maxWidth: 200,
    paddingHorizontal: 15,
    paddingTop: 10,
    paddingBottom: 15,
  },
});
